ARG TARGETPLATFORM=$TARGETPLATFORM

FROM ocaml-platform-build-$TARGETPLATFORM:latest

COPY test/tests/release.sh .

ARG TARGETOS
ENV TARGETOS=$TARGETOS
ARG TARGETARCH
ENV TARGETARCH=$TARGETARCH
ARG VERSION
ENV VERSION=$VERSION

RUN bash release.sh
